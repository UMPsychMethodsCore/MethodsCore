    echo
    echo " A command line command to drive batch processing to coregister the 'overlay'"
    echo " to fmri analyze image."
    echo
    
    . ${thisDir}/auxiliary/help/help_options_start

    echo
    echo 
    echo " e.g. "
    echo
    echo "   To coregister the 'het1overlay.img' to the first 'raprun' found in"
    echo "   the first 'run' (e.g. run_1, or run_01) of the experiment. "
    echo "   The run directory is expected to live in the 'func/' directory."
    echo 
    echo "     coregOverlay -f func/MSIT 050126zz"
    echo
    echo "   or optionaly look for the first avol image in the func/MSIT/run_01/ra_img"
    echo
    echo "     coregOverlay -w coReg8 -f func/MSIT -v arun -s ra_img 050126zz"
    echo
    echo
    echo " Current command default configuration is:"
    echo 
    
    . ${thisDir}/auxiliary/help/help_coregOverlay_Options
