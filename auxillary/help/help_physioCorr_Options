    echo "${helpComment} Sub-directory             : ${subPATH}"
    echo "${helpComment} Volume Wildcard           : ${volumeWILD}"
    echo "${helpComment} prepending name           : $outputName"
    echo "${helpComment} Number of runs to phys    : ${nRUN}"
    echo "${helpComment} fMRI TR                   : ${TR}"
    echo "${helpComment} Physio xref table         : ${PHYSIOTABLE}"
    echo "${helpComment} Physio Options            : ${PHYSIOOPTIONS}"
    
    . ${thisDir}/auxillary/help/help_options_end
