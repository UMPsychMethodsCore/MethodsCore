		  shift
		  let argn++
		  if (( $argn >= $args ))
		      then
		      echo "Missing standard volume number, aborting"
	              . ${thisDir}/auxiliary/exit_w_removal
		  else
		      standardVolume=$1
		  fi
