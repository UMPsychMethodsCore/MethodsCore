		  shift
		  let argn++
		  if (( $argn >= $args ))
		      then
		      echo "Missing parameter for functional path name, aborting"
	              . ${thisDir}/auxiliary/exit_w_removal
		  else
		      fmriPATH=$1
		  fi
