	      #
	      # run numbers
	      # 
	      "#")
		  shift
		  let argn++
		  if (( $argn >= $args ))
		      then
		      echo "Missing parameter for run number, aborting"
	              . ${thisDir}/auxiliary/exit_w_removal
		  else
		      runNAMES[$nRUN]=$1
		      let nRUN++
		  fi
		  ;;
