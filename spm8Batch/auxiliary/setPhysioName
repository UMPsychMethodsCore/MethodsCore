		  shift
		  let argn++
		  if (( $argn >= $args ))
		      then
		      echo "Missing parameter for physio xref table name, aborting"
	              . ${thisDir}/auxiliary/exit_w_removal
		  else
		      PHYSIOTABLE=$1
		  fi
