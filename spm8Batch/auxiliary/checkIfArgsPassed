# Number of arguments passed.

# We issue no error, because if the person gives no parameters then
# they want help.

if [ "$#" == "0" ]
    then
    . ${thisDir}/auxiliary/${thisCommand}_Help
    exit 0
fi

