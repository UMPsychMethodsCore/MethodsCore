		  shift
		  let argn++
		  if (( $argn >= $args ))
		      then
		      echo "Missing parameter for output name, aborting"
	              . ${thisDir}/auxiliary/exit_w_removal
		  else
		      outputName=$1
		  fi
