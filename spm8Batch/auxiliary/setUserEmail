		  shift
		  let argn++
		  if (( $argn >= $args ))
		      then
		      echo "Missing parameter for user email name, aborting"
	              . ${thisDir}/auxiliary/exit_w_removal
		  else
		      USEREMAIL=$1
		  fi
