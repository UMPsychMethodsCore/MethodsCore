		  shift
		  let argn++
		  if (( $argn >= $args ))
		      then
		      echo "Missing parameter for path name to place results, aborting"
	              . ${thisDir}/auxiliary/exit_w_removal
		  else
		      coregPATH=$1
		  fi
