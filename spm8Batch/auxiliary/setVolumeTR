		  shift
		  let argn++
		  if (( $argn >= $args ))
		      then
		      echo "Missing parameter for volume TR, aborting"
	              . ${thisDir}/auxiliary/exit_w_removal
		  else
		      TR=$1
		  fi
