		  shift
		  let argn++
		  if (( $argn >= $args ))
		      then
		      echo "Missing parameter for volume name functional, aborting"
	              . ${thisDir}/auxiliary/exit_w_removal
		  else
		      volumeWILD=$1
		  fi
