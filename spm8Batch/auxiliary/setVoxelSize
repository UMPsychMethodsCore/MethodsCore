		  shift
		  let argn++
		  if (( $argn >= $args ))
		      then
		      echo "Missing parameter for voxel size, aborting"
	              . ${thisDir}/auxiliary/exit_w_removal
		  else
		      voxelSize=$1
		  fi
