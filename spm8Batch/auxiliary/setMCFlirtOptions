		  shift
		  let argn++
		  if (( $argn >= $args ))
		      then
		      echo "Missing mcflirt options, aborting"
	              . ${thisDir}/auxiliary/exit_w_removal
		  else
		      mcOPT=$1
		  fi
